export interface LoginForm {
    email: string;
    password: string;
}

export interface RegisterFormPostulant {
    email: string;
    email_code: number;
    nombres: string;
    apellidos: string;
    password: string;
}